# 默认拒绝整个工作树，只放行生产编译真正需要的输入。
# 这样本机以后即使出现未知扩展名的导出文件、快照或凭据，也不会因为
# Dockerfile 中的 "COPY . ." 进入 builder 或 BuildKit 缓存。
**

# Go 模块与三个生产二进制的源码。
!go.mod
!go.sum
!main.go
!monitor/
monitor/**
!monitor/*.go
!monitor/*.html
!monitor/*.js
!monitor/*.css
!monitor/public/
monitor/public/**
!monitor/public/*.go
!monitor/public/*.html
!cmd/
cmd/**
!cmd/hostagent/
cmd/hostagent/**
!cmd/hostagent/*.go
!cmd/nginxcollector/
cmd/nginxcollector/**
!cmd/nginxcollector/*.go

# 测试源码不参与生产镜像编译；CI 在宿主工作树中单独执行测试。
**/*_test.go

# 第二道保险：即使未来扩大上面的放行范围，下列运行态数据和凭据仍禁止进入。
.env
.env.*
*.env
**/.env
**/.env.*
**/*.env
*.db
*.db*
*.db-journal
*.db-wal
*.db-shm
**/*.db
**/*.db*
**/*.db-journal
**/*.db-wal
**/*.db-shm
*.sqlite*
**/*.sqlite*
*.pem
*.key
*.p12
*.pfx
**/*.pem
**/*.key
**/*.p12
**/*.pfx
.local-data/
data/
secrets/
tools/
.run/
**/.run/
