# 2026-08-16 用量事实候选缺口自动修复：本机真实只读验收

## 结论

候选缺口自动修复闭环通过本机真实数据验收：候选数据不完整时未误发布、未回退前台宽扫 `logs`，旧的已签收快照持续可用；缺口被转为持久修复任务，中途重启后从断点续跑，修复后通过全范围语义复审并原子发布 90 天快照。

本报告只签收当前 P0-R 缺口修复闭环，不代表“全历史永久事实层”已完成，也不是生产上线签字。NewAPI 代码和线上数据均未修改。

## 安全边界

- 数据源经 SSH 隧道使用 `nexus_ro` 账号，只执行有界读取。
- 所有写入均发生在本机 Docker volume 的 Monitor SQLite。
- 候选修复沿用单一后台来源槽，36 个成员在同一条小时 SQL 中聚合，查询间隔 15 秒。
- 页面在修复期间继续读已发布 SQLite 快照，不回扫上游 `logs`。

## 真实运行时间线（CST）

1. 20:48:32 启动候选镜像，在 AutoMigrate 前生成并签收双库迁移快照 `main-facts-schema-20260816-v3`。
2. 20:54:20 候选发布审计发现一个完整自然日的 36 个成员缺少日语义证明；继续服务已签收 7 天快照，创建 `candidate_gap` 任务，共 864 member-hours。
3. 任务进度到 20.8% 时主动重启容器；重启后复用同一份迁移前快照，36 条修复目标和恢复水位均保留，进度从 33.3% 继续。
4. 21:01:52 修复达到 864/864。
5. 21:02:09 全范围语义复审通过，原子发布 90 天快照，发布范围为 `2026-05-18 20:00` 至 `2026-08-16 20:00`，服务 generation 只增加一次。
6. 发布后使用独立只读工具重新按小时读取修复日来源；24/24 查询完成，六组整数控制总额与 SQLite 完全一致。

## 发布后校验

- 已发布成员：36。
- 受影响日 member-day proof：36/36，非空 hash。
- 受影响日 member-hour proof：864/864，状态 complete，非空 hash。
- 独立来源对账：请求 `278=278`，退款记录 `0=0`，输入 Tokens `17,795,263=17,795,263`，输出 Tokens `136,095=136,095`，消费 quota `12,593,197=12,593,197`，退款 quota `0=0`。
- 独立工具与 facts 读取均使用 v4 用户/内部测试分类边界，但独立工具重新读源、重新累计，不复用 repair 内存结果。
- main/facts 两个 SQLite `PRAGMA quick_check` 均为 `ok`。
- 容器健康检查 200，0 OOM，0 失败重启。
- 修复完成后 Monitor 容器约 33.5 MiB / 256 MiB（13.1%）；修复中观测峰值约 55.5 MiB（21.7%）。
- 最终本机镜像 digest：`sha256:346c8db9bc637a04c9911a495e8cc6ad79eb6b73cf1afbfd1a63c0338bb7742f`。
- 最终容器重建后保留 90 天已发布快照，下一个闭合小时正常滑动为 `2026-05-18 21:00` 至 `2026-08-16 21:00`。

## 代码验证

- `go test ./... -count=1`：通过。
- `go test -race ./monitor -count=1`：通过，82.869s。
- `go vet ./...`：通过。
- `git diff --check`：通过。
- 页面内联 JavaScript `node --check`：通过。
- 新增用例覆盖：候选缺口自动创建、去重、精确目标、旧快照继续可读、零前台回源、来源失败持久化、成员变更安全取消、完成后发布。

## 未签收项

- 当前会话没有可连接的浏览器实例，因此管理员进度条和真实登录页面的人工视觉检查待补；后端状态、页面 JS 语法和容器制品已验证。
- 全历史永久事实层、按用户来源起点、日级冷历史导入、长期 repair job 和独立备份目标属于下一开发阶段，不应把当前 90 天验收误称为全历史完成。
