# 经营核算性能优化：提交前复核

日期：2026-09-24。范围：本机候选核验，不授权生产部署。

## 候选与边界

- 在隔离工作树 `fix/finance-cold-read-v11714` 核验；已提交基础为 `f9b06e5`，本次待提交的是第二至第五轮优化和测试、文档收尾。与 `v1.17.13` 比较，完整候选还包含此前第一轮异步读取、只读连接与快照优化。
- 本次只读查询到的远端 main 为 `ce0305c`，它是候选祖先；远端最高 1.17 系列正式 tag 为 `v1.17.13`。这是 Git 基线核验，不替代部署前核实线上实际镜像及配置。
- 运行修改限于经营核算读取、缓存、计算复用及其状态展示；共用改动为 Monitor 退出时停止财务队列、两个默认关闭开关和页面局部容器。未修改 NewAPI、认证、采集协议、业务数据库表结构、生产配置或流量。
- 原开发工作树的其他未提交内容没有搬入或暂存。已有离线赠送工具属于核算维护源码，继续受版本管理；没有新增 HTTP/启动调用，不会随 Monitor 自动运行。
- 本轮不补齐全部历史数据，不实现四维报表或增量赠送检查点；优化不能把缺失成本、赠送证据或 CUR 变成完整利润。

## 本机验证结果

| 验证项 | 结果与限制 |
| --- | --- |
| 全量 Go 回归 | `go test ./... -count=1 -timeout 15m` 通过，本轮复检 monitor 包约 241.921 秒；可选真实副本验收另见性能实施记录。 |
| CI 工具链补测 | Go 1.26.6 上重新运行账单复用、历史活动/缓存失效、月账共享、赠送流式读取的专项测试，通过（1.858 秒）。完整 Linux 平台回归仍由 CI 执行。 |
| 经营核算竞态 | 第五轮全部经营核算与赠送读取 race 通过，另追加主库 WAL/零时间/历史失效测试。之后未改运行代码。 |
| 相邻功能与共享路径竞态 | 渠道、上游、用户用量、Portal、稳定性、基础设施、容量、模型、登录、来源、存储等前缀及财务只读/快照测试共 522 项，整组 `-race` 通过，448.816 秒。不是全部 1,678 项 monitor 测试的全量 race；完整分片仍由 CI 执行。 |
| 前端 | 108 项 Node 测试通过，包含本机离线赠送报表验收产物。 |
| 静态与策略 | `go vet ./...`、`git diff --check`、667 个 Linux 生产/测试包的依赖策略检查通过；1,678 项 monitor 测试均唯一落入 CI race 分片。 |
| 密钥扫描 | CI 固定版本 Gitleaks 8.30.0 的正/负控制通过；工作树扫描及全部 214 个可见历史提交扫描均未发现泄漏。备份和原始证据未进入工作树。 |
| Go 漏洞扫描 | 使用 CI 相同的 Go 1.26.6 / govulncheck 1.1.4 扫描 21 个根包、66 个模块：可达漏洞 0，导入包漏洞 0。仅有模块级 GO-2026-5932（未使用的 `golang.org/x/crypto/openpgp`）；依赖策略已验证未导入该包，不因此删除仍使用的整个 x/crypto 模块。本次无依赖变更。 |
| 金额与覆盖 | 第五轮同一核验小时、143 天/5 个月真实闭合副本，新旧完整公开响应仅生成时间不同；金额、空值、明细、覆盖、配对与 CUR 都比较。 |
| 持续读写 | 第五轮一分钟/10 客户端，1,190 个响应、599 次模拟事实库提交，零读写错误、零报表差异，P95 26.92 ms；任务峰值 1，WAL 收尾 TRUNCATE 通过。30 分钟长测属于第一轮，不冒称最终代码重跑了 30 分钟。 |
| 隔离 Docker | Go 1.26.6 离线构建，1 CPU/768 MiB，internal 网络；原始备份与其他预览未改。已有快照、冷区间 202→200、刷新、重启恢复已在迭代验收。 |
| 页面交叉回归 | 本轮重新遍历 11 个 tab；每次仅目标 tab 可见，其他模块未误嵌入财务隐藏容器，未出现页面脚本异常或 HTTP 4xx/5xx。临时 Chrome 已关闭。 |
| 构建过滤 | 本机断网构建仓库既有 context-check 镜像并检查：新增三个运行文件完整；`cmd/financegiftlocal`、`dev`、`local-data`、`tools`、`.git`、`.env` 不在上下文。 |

性能数据仅为本机闭合副本观测，不保证生产响应时间。隔离容器禁止生产采集，精确渠道成本路径在原始副本处理器验收而非该容器启用；存在原有历史完整性提示。因此页面回归不能冒充线上采集与全部业务事实完整性验收。

## 本次收尾

- 修正旧运维说明中“三分钟后转前台计算、手动刷新绕过快读”的过时描述，统一为当前有界队列与 48 小时展示上限。仅更新说明，不改变运行行为。
- 明确离线维护源码可提交与离线命令不部署的区别，避免误删依赖代码或把备份当源码提交。
- 首次漏洞扫描在本机 Go 1.27.1 下发生工具自身 panic，未将其记为通过；切换 CI 指定的 Go 1.26.6 后完成扫描并查看详细结果。未因此修改应用代码或升级依赖。

## 提交与发布门槛

本机未发现阻断提交的功能回归，可以提交为发布候选，但不能凭本机结果直接宣称生产已通过验收。

正式发布仍要求新候选的 GitHub CI 全部通过：Linux 全部 race 分片、lint、历史密钥扫描、可达漏洞检查及精确待发布镜像的安全扫描。本机无新增依赖不等于已确认没有漏洞。

只在用户授权后打新 tag/推送并使用固定版本镜像；部署前核实实际旧镜像、配置、备份与恢复检查。本轮无表结构变更；快读和只读隔离分别默认关闭，可关闭相应开关重启 Monitor 或恢复旧镜像，不通过删库/删缓存回滚。上线仅变更 Monitor，按只读隔离、异步报表顺序验收，保留其他模块和上游采集观察；稳定后才合入 main。
