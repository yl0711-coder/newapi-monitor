# 历史赠送分组补齐：真实样本离线验收

## 范围

用户同意继续后，只读取一个真实用户小时：用户 ID 45，北京时间 2026-06-01 17:00–18:00。生产无写入、未修改配置、未触发批量任务。成功修复发生在测试创建的独立临时 SQLite 副本，不是生产或 8112 预览数据卷；测试后临时副本自动清理。

## 来源读取保护

- 复用已有访问方式，经 Ubuntu-Monitor 连接现有生产 Monitor 指向的 RDS，账号严格限定 `monitor_ro`。
- 独立 loopback 端口 13326 和私有 SSH control socket，没有启用已有 13316 端口，因此不会让其他本地容器意外恢复来源查询。
- 先执行普通 EXPLAIN（不是 ANALYZE），只接受有索引的 ref/range/const 单表计划，估算扫描不超过 10,000 行。未通过则不执行数据 SELECT。
- 一个用户、一个闭合小时，最多 100 条；MySQL 查询限时 2 秒，整体上下文 8 秒；显式只读、可重复读事务。
- 实际返回 11 条。仅保存 id、user_id、created_at、type、quota、group；沿用统一的自动测试排除谓词，不导出 content、other、令牌、密码、Prompt 或响应体。
- 查询后关闭专用隧道；已确认 13326 没有监听。生成的临时凭证配置副本已删除。

## 结果

| 核对项 | 修复前 | 本地副本修复后 |
| --- | --- | --- |
| 请求顺序记录 | 11 条 | 11 条，原 ID/时间/类型/用户逐条一致 |
| 原扣额 quota | 7,944 | 7,944，不变 |
| 原退款 quota | 0 | 0，不变 |
| 历史分组未知 | 11 条 | 0 条 |
| 所验时段赠送证据完整性 | false | true |
| 可核验赠送实际消耗 | 不发布 | 15,888 microUSD，即 $0.015888 |
| 重复执行 | — | 零更新、不重新访问来源 |
| 全部用户金额事实及主同步游标 | 原值 | 与修复前深度比较一致 |

该样本的消费由赠送额度覆盖，不应当作付费收入。这只是一个用户小时的结论，不代表全站历史收入/利润已经完整，不能按这个比例估计其他用户的赠送消费。

## 可复现性与测试边界

- 私有原始样本：`/private/tmp/finance-gift-real.Qmvfkf/evidence.json`（0600），不提交 Git。SHA-256：`7362c30ac246c36d5e6bac93d0a9e0b6f4fe32652abda863585ec8cf8a864933`。
- 离线真实样本测试：`TestFinanceGiftScopeRepairRealEvidenceOnLocalClone`。设置 `MONITOR_GIFT_SCOPE_REPAIR_SNAPSHOT`、`MONITOR_GIFT_SCOPE_REPAIR_MAIN_SNAPSHOT`、`MONITOR_GIFT_SCOPE_REPAIR_EVIDENCE` 为已下载备份及私有样本路径后执行；它没有网络查询逻辑。
- 与缺来源保留旧数据用例一起通过，耗时 0.968 秒。赠送修复、历史兼容、缓存指纹及导出计划门禁的定向 race 通过（Monitor 13.282 秒，导出工具 1.537 秒）；只读工具 vet 和 diff check 通过。
- 本地 `tools/readonly-inspect` 原本属于 Git 忽略的运维辅助目录；本次增加的只读导出模式仍只供本机操作，不混入正式发布。可提交的离线验收测试使用 JSON 文件，不依赖该忽略目录才能编译或运行普通 CI。
- 没有重跑完整仓库测试、没有新增页面、没有重建预览容器，不宣称已经完成批量修复功能或发布验收。

## 下一小步

真实来源已证明至少这一旧小时可取回，后续不必重跑全部历史。下一步是小批次调度：按既有缺口选择少量用户小时，限定查询频率、失败停止/跳过策略及暂停条件，保存逐小时验收记录；先在本地副本验证，再决定是否扩大范围。不能因为一个样本成功就直接启动 915 个用户小时的生产读取或线上修复。
