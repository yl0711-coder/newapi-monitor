# 内部成本未核验原因拆解（本机）

## 本地副本结论

沿用 2026-09-20 下载的本地 SQLite 数据，查询北京时间 9 月 1—19 日，未访问生产。独立只读 SQL 重建自动测试、内部账号及非业务分组的渠道小时集合，得到 19,062 项，与本机接口一致。

| 原因 | 渠道小时项 | 含义 |
| --- | ---: | --- |
| 尚无渠道成本发布 | 18,669 | 当前有效经济账本中没有这个渠道小时的成本发布记录，不能自动当零或认为重算即可修好 |
| 渠道上游成本缺失 | 393 | 当前记录明确为 `upstream_cost_missing`，仍不能证明该渠道的成本归属；不等同于上游原始日志不存在 |

这不是最新线上状态，也不是请求次数或新增业务报错。

该本地窗口的细粒度成本证据仅见 4sapi：2,424 条证据、413 个小时、10 个匿名来源；`channel_cost_source_bindings` 表为 0 行。代码要求同账户周期、匿名来源、有效时间范围内的 confirmed+allocated 绑定，才能把成本归入本地渠道。没有绑定时成本保持未归属，渠道侧可能显示上游成本缺失。不能按分组同名、请求比例或当前配置猜测历史归属。

其余账户即使已有余额/汇总账单，也不等于已有支持内部成本扣减的渠道细粒度发布。这两条数据链需区别处理。未配置上游不发起补采。

## 本轮实现

- 利用内部成本读取时已经取得的有效发布记录和小时校验结果，增加紧凑诊断索引，无新增 SQL 或上游请求。
- 区分尚无发布、渠道成本缺失、小时账本未通过校验、成本记录未通过校验、多份可用记录等原因；不更改原严格/混用/未核验选择条件。
- 原因写入内存事件；月/日子范围重新汇总。旧事件无原因归入 `unspecified`，不能丢掉未核验数量。
- 日响应新增 `internal_cost_unverified_reasons`，页面在原内部成本列下用小字展示；未知原因显示通用文案，不把任意后台字符串作为 HTML 输出。
- 不修改任何金额、完整性门槛、渠道绑定、数据库结构或采集范围。月缓存 schema 16，报表键 `daily-internal-diagnostics-v1`，finance.js v53。

## 本机验收

- 分类优先级、SQL 实际读取、逐日归因与数量守恒、跨日隔离、旧事件兼容、来源对象不被修改均有测试。
- 定向竞态回归通过（31.138s）；全仓 vet、格式检查通过。
- 前端 100 项：99 通过、1 项依赖私有产物跳过、0 失败。
- CI 竞态清单 1,628 项唯一覆盖，分片 281 / 339 / 364 / 383 / 261。
- 全仓 `go test ./... -count=1 -timeout 15m` 通过，Monitor 230.439s。
- 真实副本接口与独立 SQL 的 18,669 + 393 完全一致；19 天每天的原因合计等于原未核验总数。除新增原因对象外，原日字段、总表、月表、供应商成本、赠送及上游覆盖均深度相等；渠道汇总、逐账户消费和余额不变。
- 缓存/重算一致；单次本机重算 1837ms、缓存 71ms、渠道 250ms，不是压测或生产承诺。
- 独立无头 Chrome 仅允许 127.0.0.1 请求，操作日期、刷新、展开明细；1440/900 宽度下 19 行、17 列，逐日原因文案与接口相符，无运行异常。未做人工视觉验收。

只更新 `finance-round-one-20260921`，数据卷保留；空 DSN、快照模式、internal 网络无默认路由；running/restart 0/OOM false。未操作 8100，没有提交、tag、生产部署或生产读取。

预览：`http://127.0.0.1:8112/#tab=finance`。

镜像 `newapi-monitor:finance-internal-diagnostics-local-20260922`，索引 SHA `f5eb4cb1b6900a61947ce1045abe7ab7463e3009e4d257e6730d23d081316b1c`。复用本机运行层，不等于正式发布镜像扫描。

私有前后响应、只读审计 SQL、脚本和测试日志：`/private/tmp/finance-internal-diagnostics.R3H5EL`，不进 Git。

## 下一轮只做什么

优先核对 4sapi 的 10 个匿名来源是否有可验证的历史渠道归属，明确有效时间和账户周期；证据足够再考虑在隔离副本建立绑定并重算对账。证据不足应列出所缺信息，不擅自配置真实绑定、不扩大采集、不自动访问上游。不能用本轮“缺口可解释”替代“缺口已修复”或完整利润结论。
